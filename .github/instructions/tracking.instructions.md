# tracking/ instructions

Path focus: `tracking/**`

- Temporal identity continuity, missing detections, swap detection, and trajectory integrity.
- Keep raw extractor outputs immutable; write derived tracked outputs separately.
